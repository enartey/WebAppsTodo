{{! Will be called with a model object }}
{{! Loaded directly in controller.js }}

{{! TODO: Input box with value from x }}
<input type="input" value="{{x}}" />

{{! TODO: Operator pulldown list }}
<select>
	<option value="{{operator}}">{{operator}}</option>
</select>

{{! TODO: Input box with value from y }}
<input type="input" value="{{y}}" />

{{! TODO: Equal sign }}
{{! TODO: Result }}
